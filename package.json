{
  "name": "hello",
  "version": "1.0.0",
  "description": "",
  "main": "null",
  "scripts": {
    "build": "go build",
    "predev": "npm run build",
    "dev": "./gosdk $(cat .env)",
    "dev:watch": "nodemon -e go --watch ./ --exec npm run dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.12.1"
  }
}
